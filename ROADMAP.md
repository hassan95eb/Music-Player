# نقشه‌ی راه

هر فاز ≈ یک روز کاری. هر فاز مستقل قابل commit و قابل تست است.

---

## ✅ فاز ۰ — پایه (انجام شد)

- [x] مهاجرت به Next.js 16 + TypeScript + Tailwind v4، ساختار پوشه‌ها
- [x] `lib/types.ts`، `lib/format.ts`، `lib/constants.ts`، `lib/settings.ts`
- [x] `db/tracks.ts` — لایه IndexedDB
- [x] `lib/metadata.ts` — پارسر ID3v2 (عنوان/آرتیست/آلبوم/کاور) + خواندن duration
- [x] `store/PlayerProvider.tsx` — کتابخانه، پخش، seek، next/prev، تنظیمات
- [x] UI حداقلی: UploadZone، Cover، TrackMeta، ProgressBar، Controls
- [x] `next build` بدون خطا

**خروجی:** آهنگ آپلود می‌شود، پخش می‌شود، بعد از رفرش باقی می‌ماند.

---

## فاز ۱ — پلی‌لیست و جستجو

- [ ] `components/playlist/PlaylistItem.tsx` — کاور کوچک، عنوان، آرتیست، مدت، دکمه حذف
- [ ] `components/playlist/Playlist.tsx` — لیست مجازی‌نشده (فعلاً ساده)، هایلایت آهنگ فعال
- [ ] `components/playlist/SearchBar.tsx` — فیلتر روی عنوان/آرتیست/آلبوم با debounce
- [ ] چیدمان دو ستونی: پلیر سمت چپ، پلی‌لیست سمت راست (روی موبایل تب‌شونده)
- [ ] حالت خالی و حالت «نتیجه‌ای پیدا نشد»
- [ ] `UploadZone` به‌صورت compact بالای پلی‌لیست
- [ ] **تست:** حذف آهنگِ در حال پخش نباید پلیر را بشکند

**نکته‌ی پیاده‌سازی:** ترتیب پخش باید بر اساس لیستِ *فیلترنشده* باشد،
وگرنه با فعال بودن سرچ، `next()` رفتار عجیبی می‌گیرد.

---

## فاز ۲ — Shuffle، Repeat و کنترل صدا

- [ ] `components/player/VolumeControl.tsx` — اسلایدر + دکمه mute (آیکون بر اساس سطح صدا)
- [ ] دکمه‌های shuffle و repeat در `Controls.tsx` (repeat: off → all → one)
- [ ] جایگزینی شافل تصادفی فعلی با **صف شافل‌شده** (Fisher–Yates) تا آهنگ‌ها تکراری نشوند
- [ ] `previous()` در حالت شافل باید تاریخچه‌ی پخش را برگردد، نه رندوم جدید
- [ ] ذخیره‌ی volume/muted/shuffle/repeat در localStorage (زیرساختش آماده است)
- [ ] **تست:** repeat=one، repeat=all روی آخرین آهنگ، off روی آخرین آهنگ

---

## فاز ۳ — دارک مود و رنگ داینامیک از کاور

- [ ] `components/theme/ThemeProvider.tsx` — light/dark/system، بدون فلش اولیه
      (اسکریپت inline در `layout.tsx` که قبل از hydration کلاس را ست کند)
- [ ] `components/theme/ThemeToggle.tsx`
- [ ] `lib/color.ts` — استخراج رنگ غالب از کاور با canvas (نمونه‌برداری + تجمیع)
- [ ] پس‌زمینه‌ی gradient/بلور بر اساس رنگ کاور (معادل `.background` در نسخه‌ی قدیمی)
- [ ] بررسی کنتراست: اگر رنگ خیلی روشن/تیره بود، متن را برعکس کن
- [ ] **تست:** آهنگ بدون کاور، و کاور تک‌رنگ

---

## فاز ۴ — کیبورد شورتکات و یکپارچگی با سیستم‌عامل

- [ ] `hooks/useKeyboardShortcuts.ts`
      - `Space` پخش/توقف، `←/→` جابه‌جایی ۵ ثانیه، `↑/↓` صدا
      - `N`/`P` بعدی/قبلی، `M` بی‌صدا، `S` شافل، `R` تکرار، `/` فوکوس روی سرچ
      - وقتی فوکوس داخل input است، شورتکات نباید عمل کند
- [ ] `hooks/useMediaSession.ts` — عنوان/آرتیست/کاور روی کنترل‌های سیستم‌عامل و هدفون
- [ ] راهنمای شورتکات‌ها (مودال با کلید `?`)
- [ ] **تست:** با صفحه‌کلید فقط، بدون ماوس، همه‌ی کارها انجام شود

---

## فاز ۵ — جمع‌بندی و کیفیت

- [ ] دسترس‌پذیری: `aria-label`ها، ترتیب فوکوس، `prefers-reduced-motion`
- [ ] رفتار موبایل: safe-area، تاچ روی نوار پیشرفت
- [ ] Error boundary + پیام واضح وقتی IndexedDB در دسترس نیست (حالت private browsing)
- [ ] نمایش حجم استفاده‌شده و دکمه‌ی «پاک کردن کتابخانه»
- [ ] تست‌های واحد برای `format.ts` و `metadata.ts` (Vitest)
- [ ] بهینه‌سازی: `React.memo` روی آیتم‌های پلی‌لیست، مجازی‌سازی اگر > ۲۰۰ آهنگ
- [ ] دیپلوی روی Vercel + آپدیت README

---

## ایده‌های بعدی (خارج از محدوده‌ی فعلی)

- ویژوالایزر با Web Audio API (`AnalyserNode`)
- اکولایزر و کنترل سرعت پخش
- پلی‌لیست‌های چندگانه و مرتب‌سازی drag & drop
- ایمپورت/اکسپورت کتابخانه به‌صورت JSON
- PWA + آفلاین کامل
